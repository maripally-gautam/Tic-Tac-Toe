import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.tictactoe.arcade",
  appName: "Tic Tac Toe Arcade",
  webDir: "dist",
  android: {
    backgroundColor: "#0c0908",
  },
};

export default config;
